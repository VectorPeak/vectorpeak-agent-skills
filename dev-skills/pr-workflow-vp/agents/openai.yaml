interface:
  display_name: "PR Workflow"
  short_description: "Find and draft small GitHub PRs"
  default_prompt: "Fork and clone a GitHub project, find a small bug-fix PR candidate, inspect project contribution rules with multiple agents, draft the PR by default, and only submit it after I explicitly confirm."
